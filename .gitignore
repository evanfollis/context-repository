# Legacy abstract-layer artifacts from prior identity (pre-2026-04-16)
# These files were never committed and represent wrong-direction work.
# Not deleted to preserve local disk state; excluded from tracking.
docs/architecture_rubric.md
docs/governance_contract.md
docs/repo_bootstrap_template.md
docs/telemetry_contract.md
docs/venture_governance.md
notes/
schemas/venture_controller.md

# NOTE: `spec/` was listed here until 2026-07-12. It was ignored to bury the
# prior identity's abstract-schema work — but `spec/discovery-framework/` (the
# L1 canon: obligations model, validator rules, the eight JSON Schemas) was
# later built underneath that path and silently inherited the ignore. The
# highest truth source in the system was therefore untracked for ~3 months:
# undiffable, unreviewable, unrevertable. Do not re-add it. If a path under
# spec/ ever needs ignoring, ignore that path, never the root.

__pycache__/
*.pyc
